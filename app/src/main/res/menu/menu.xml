<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/settings"
        android:icon="@drawable/ic_action_setting_icon"
        android:title="@string/settings_name"
        app:showAsAction="never">
    </item>

    <item
        android:id="@+id/contact"
        android:icon="@drawable/ic_action_phone"
        android:title="@string/contact_name"
        app:showAsAction="never">
    </item>

    <item
        android:id="@+id/about"
        android:icon="@drawable/ic_action_help_icon"
        android:title="@string/about_name"
        app:showAsAction="never">
    </item>

    <item
        android:id="@+id/logout"
        android:icon="@drawable/ic_action_logout"
        android:title="@string/logout_name"
        app:showAsAction="never">
    </item>

    <item
        android:id="@+id/search"
        android:icon="@drawable/ic_action_search_icon"
        android:title="@string/search_name"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="ifRoom"
        tools:ignore="AlwaysShowAction">
    </item>


</menu>